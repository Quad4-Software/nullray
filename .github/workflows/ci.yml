# CI: build, package tests, self-test, CLI smoke.
#
# Harden-Runner first in every job:
#   https://docs.stepsecurity.io/github-actions/harden-runner
#
# Secure-use: least-privilege GITHUB_TOKEN, pinned action SHAs, no
# pull_request_target, concurrency cancel-in-progress.
#
# Pinned actions (bump tag and SHA together):
#   step-security/harden-runner@v2.21.1  e14015d583714f6e62063499dc959a02595150a1
#   actions/checkout@v7.0.1             3d3c42e5aac5ba805825da76410c181273ba90b1
#   actions/cache@v6.1.0                55cc8345863c7cc4c66a329aec7e433d2d1c52a9

name: ci

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (${{ matrix.suite }})
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - suite: ui
            path: nullray/ui
          - suite: agent
            path: nullray/agent
          - suite: tools
            path: nullray/tools
          - suite: skills
            path: nullray/skills
          - suite: session
            path: nullray/session
          - suite: store
            path: nullray/store
          - suite: sandbox
            path: nullray/sandbox
          - suite: mcp
            path: nullray/mcp
          - suite: provider
            path: nullray/provider
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y build-essential clang llvm make

      - name: Cache Odin toolchain
        id: cache-odin
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/Odin
            ~/.cache/odin
          key: odin-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('packaging/odin-pin') }}-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            odin-${{ runner.os }}-${{ runner.arch }}-

      - name: Ensure Odin
        run: |
          set -euo pipefail
          chmod +x scripts/ensure-odin.sh
          bash scripts/ensure-odin.sh "$HOME/Odin"
          echo "$HOME/Odin" >> "$GITHUB_PATH"

      - name: Build TLS static lib
        run: make tls-lib

      - name: Package tests
        env:
          SUITE_PATH: ${{ matrix.path }}
        run: odin test "$SUITE_PATH" -collection:nullray=nullray -define:ODIN_TEST_THREADS=1

  smoke:
    name: build and smoke
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs: test
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y build-essential clang llvm make

      - name: Cache Odin toolchain
        id: cache-odin
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/Odin
            ~/.cache/odin
          key: odin-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('packaging/odin-pin') }}-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            odin-${{ runner.os }}-${{ runner.arch }}-

      - name: Ensure Odin
        run: |
          set -euo pipefail
          chmod +x scripts/ensure-odin.sh
          bash scripts/ensure-odin.sh "$HOME/Odin"
          echo "$HOME/Odin" >> "$GITHUB_PATH"

      - name: Build nullray
        run: make

      - name: Smoke
        run: |
          ./bin/nullray --self-test
          ./bin/nullray --help | head -5
          ./bin/nullray --version
          ./bin/nullray --completions bash | head -3
          ./bin/nullray --man | head -3
          chmod +x scripts/print-smoke.sh
          ./scripts/print-smoke.sh

  smoke-linux-arm64:
    name: build and smoke (linux arm64)
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 60
    needs: test
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y build-essential clang llvm make

      - name: Cache Odin toolchain
        id: cache-odin
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/Odin
            ~/.cache/odin
          key: odin-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('packaging/odin-pin') }}-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            odin-${{ runner.os }}-${{ runner.arch }}-

      - name: Ensure Odin
        run: |
          set -euo pipefail
          chmod +x scripts/ensure-odin.sh
          bash scripts/ensure-odin.sh "$HOME/Odin"
          echo "$HOME/Odin" >> "$GITHUB_PATH"

      - name: Build nullray
        run: make

      - name: Smoke
        run: |
          ./bin/nullray --self-test
          ./bin/nullray --version
          ./bin/nullray --doctor | head -20

  smoke-macos:
    name: build and smoke (macos)
    runs-on: macos-latest
    timeout-minutes: 60
    needs: test
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install deps
        run: brew install llvm

      - name: Cache Odin toolchain
        id: cache-odin
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/Odin
            ~/.cache/odin
          key: odin-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('packaging/odin-pin') }}-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            odin-${{ runner.os }}-${{ runner.arch }}-

      - name: Ensure Odin
        run: |
          set -euo pipefail
          chmod +x scripts/ensure-odin.sh
          bash scripts/ensure-odin.sh "$HOME/Odin"
          echo "$HOME/Odin" >> "$GITHUB_PATH"

      - name: Build and smoke
        run: |
          make
          ./bin/nullray --self-test
          ./bin/nullray --version
          chmod +x scripts/print-smoke.sh
          ./scripts/print-smoke.sh

  smoke-windows:
    name: build and smoke (windows)
    runs-on: windows-latest
    timeout-minutes: 60
    needs: test
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Cache Odin
        id: cache-odin
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/Odin
          key: odin-windows-release-dev-2026-09-mbedtls1
          restore-keys: |
            odin-windows-release-

      - name: Install LLVM
        shell: pwsh
        run: |
          choco install llvm -y
          Add-Content -Path $env:GITHUB_PATH -Value "C:\Program Files\LLVM\bin"

      - name: Install Odin release
        shell: pwsh
        run: |
          if (Test-Path "$env:USERPROFILE\Odin\odin.exe") {
            Write-Host "using cached odin"
          } else {
            $url = "https://github.com/odin-lang/Odin/releases/download/dev-2026-09/odin-windows-amd64-dev-2026-09.zip"
            $zip = "$env:RUNNER_TEMP\odin.zip"
            Invoke-WebRequest -Uri $url -OutFile $zip
            Remove-Item -Recurse -Force "$env:USERPROFILE\Odin" -ErrorAction SilentlyContinue
            New-Item -ItemType Directory -Path "$env:USERPROFILE\Odin" | Out-Null
            Expand-Archive -Path $zip -DestinationPath "$env:USERPROFILE\Odin" -Force
            $exe = Get-ChildItem "$env:USERPROFILE\Odin" -Recurse -Filter odin.exe | Select-Object -First 1
            if ($null -eq $exe) { throw "odin.exe not found in release zip" }
            if ($exe.Directory.FullName -ne "$env:USERPROFILE\Odin") {
              Copy-Item -Path "$($exe.Directory.FullName)\*" -Destination "$env:USERPROFILE\Odin" -Recurse -Force
            }
          }
          Add-Content -Path $env:GITHUB_PATH -Value "$env:USERPROFILE\Odin"

      - name: Build TLS static lib
        shell: pwsh
        run: .\scripts\build-tls-lib.ps1

      - name: Build and smoke
        shell: cmd
        run: |
          where odin
          where clang
          if not exist bin mkdir bin
          odin build cmd\nullray -out:bin\nullray.exe -collection:nullray=nullray -extra-linker-flags:"-lws2_32" -define:NULLRAY_BUILD_DATE=2026-09-07 -define:NULLRAY_BUILD_TIME=00:00:00
          if errorlevel 1 exit /b 1
          bin\nullray.exe --self-test
          if errorlevel 1 exit /b 1
          bin\nullray.exe --version
          if errorlevel 1 exit /b 1

      - name: Print smoke
        shell: pwsh
        run: .\scripts\print-smoke.ps1
